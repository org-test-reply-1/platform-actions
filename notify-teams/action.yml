name: Notify Teams
description: 'Sends a notification to Microsoft Teams'

inputs:
  webhook-url:
    description: 'Teams webhook URL'
    required: true
  title:
    description: 'Message title'
    required: true
  message:
    description: 'Message body'
    required: true
  color:
    description: 'Message color (green, red, orange)'
    required: false
    default: 'green'
  attachment:
    description: 'Additional text sent as a second message'
    required: false
    default: ''

runs:
  using: composite
  steps:
    - name: Send Teams notification
      shell: bash
      env:
        WEBHOOK_URL: ${{ inputs.webhook-url }}
        TITLE: ${{ inputs.title }}
        MESSAGE: ${{ inputs.message }}
        ATTACHMENT: ${{ inputs.attachment }}
      run: |
        case "${{ inputs.color }}" in
          red)    COLOR="FF0000" ;;
          orange) COLOR="FFA500" ;;
          *)      COLOR="00FF00" ;;
        esac

        # Escape for JSON
        TITLE_ESC=$(echo "$TITLE" | sed 's/"/\\"/g')
        MESSAGE_ESC=$(echo "$MESSAGE" | sed 's/"/\\"/g' | sed ':a;N;$!ba;s/\n/\\n/g')

        curl -s -X POST "${WEBHOOK_URL}" \
          -H "Content-Type: application/json" \
          -d "{
            \"type\": \"message\",
            \"attachments\": [{
              \"contentType\": \"application/vnd.microsoft.card.adaptive\",
              \"content\": {
                \"\$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",
                \"type\": \"AdaptiveCard\",
                \"version\": \"1.2\",
                \"body\": [
                  {
                    \"type\": \"TextBlock\",
                    \"text\": \"${TITLE_ESC}\",
                    \"weight\": \"Bolder\",
                    \"size\": \"Medium\",
                    \"color\": \"$([ "${{ inputs.color }}" = "red" ] && echo "Attention" || echo "Good")\"
                  },
                  {
                    \"type\": \"TextBlock\",
                    \"text\": \"${MESSAGE_ESC}\",
                    \"wrap\": true
                  }
                ]
              }
            }]
          }"

        if [ -n "${ATTACHMENT}" ]; then
          ATTACH_ESC=$(echo "$ATTACHMENT" | sed 's/"/\\"/g' | sed ':a;N;$!ba;s/\n/\\n/g')
          curl -s -X POST "${WEBHOOK_URL}" \
            -H "Content-Type: application/json" \
            -d "{
              \"type\": \"message\",
              \"attachments\": [{
                \"contentType\": \"application/vnd.microsoft.card.adaptive\",
                \"content\": {
                  \"\$schema\": \"http://adaptivecards.io/schemas/adaptive-card.json\",
                  \"type\": \"AdaptiveCard\",
                  \"version\": \"1.2\",
                  \"body\": [
                    {
                      \"type\": \"TextBlock\",
                      \"text\": \"Detailed summary\",
                      \"weight\": \"Bolder\"
                    },
                    {
                      \"type\": \"TextBlock\",
                      \"text\": \"${ATTACH_ESC}\",
                      \"wrap\": true
                    }
                  ]
                }
              }]
            }"
        fi
